const UserOrderHistory = () => {
    return (
        <div>
            <h2>Your Order History</h2>
            {/* Render user's order history here */}
        </div>
    );
};

export default UserOrderHistory;
