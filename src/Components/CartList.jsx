import CartCard from "./CartCard";
const CartList = () => {
    return (
        <div className="cart-list">
            <CartCard />
            <CartCard />
            <CartCard />
        </div>
    );
};

export default CartList;
